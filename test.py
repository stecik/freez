from Freez import WinManager, CursesManager

# win_man = WinManager()
# win_man.list()


# import psutil


# def get_executable_from_pid(pid):
#     try:
#         process = psutil.Process(pid)
#         return process.exe()  # Get the executable path
#     except psutil.NoSuchProcess:
#         return f"Error: No process found with PID {pid}"
#     except psutil.AccessDenied:
#         return f"Error: Permission denied for PID {pid}"
#     except Exception as e:
#         return f"Error: {str(e)}"


# # Example usage
# pid = 5090  # Replace with your actual PID
# executable_path = get_executable_from_pid(pid)
# print(f"Executable for PID {pid}: {executable_path}")
